{
  PerFieldAnalyzerWrapper analyzer=new PerFieldAnalyzerWrapper(new AnsjAnalyzer(TYPE.index));
  Directory directory=null;
  IndexWriter iwriter=null;
  IndexWriterConfig ic=new IndexWriterConfig(analyzer);
  directory=new RAMDirectory();
  iwriter=new IndexWriter(directory,ic);
  addContent(iwriter,"??????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????10????????????????????????????????????");
  addContent(iwriter,"??????????????????????????????????????????????????????");
  addContent(iwriter,"???????????????");
  iwriter.commit();
  iwriter.close();
  System.out.println("??????????????????");
  Analyzer queryAnalyzer=new AnsjAnalyzer(AnsjAnalyzer.TYPE.dic);
  System.out.println("index ok to search!");
  search(queryAnalyzer,directory,"\"??????\"");
}
