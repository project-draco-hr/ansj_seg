{
  Term term=null;
  mergerFreq(root,0);
  for (int i=0; i < terms.length; i++) {
    term=terms[i];
    while (term != null && term.getFrom() != null && term != end) {
      int to=term.getToValue();
      mergerFreq(term,to);
      term=term.getNext();
    }
  }
  optimalRoot();
}
