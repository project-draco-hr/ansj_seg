{
  Term term=null;
  for (int i=0; i < terms.length; i++) {
    term=terms[i];
    while (term != null) {
      System.out.print(term + ":" + term.getScore()+ "\t");
      term=term.getNext();
    }
    System.out.println();
  }
}
