{
  List<Term> all=new LinkedList<Term>();
  Term term=null;
  int length=terms.length - 1;
  for (int i=0; i < length; i++) {
    term=terms[i];
    if (term == null) {
      continue;
    }
    all.add(term);
  }
  return all;
}
