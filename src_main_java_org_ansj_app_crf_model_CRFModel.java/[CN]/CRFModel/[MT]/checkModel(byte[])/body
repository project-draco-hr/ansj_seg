{
  try {
    InputStream inputStream=new ObjectInputStream(new GZIPInputStream(new ByteArrayInputStream(bytes)));
    byte[] b=new byte[bytes.length];
    inputStream.read(b);
    String string=new String(b);
    if (string.startsWith("ansj1")) {
      return true;
    }
 else {
      return false;
    }
  }
 catch (  Exception e) {
    return false;
  }
}
