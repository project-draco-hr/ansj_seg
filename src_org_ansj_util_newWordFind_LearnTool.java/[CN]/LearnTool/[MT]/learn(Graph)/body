{
  if (isCompany) {
    findCompany(graph);
  }
  if (isAsianName) {
    findAsianPerson(graph);
  }
  if (isForeignName) {
    findForeignPerson(graph);
  }
  if (isNewWord) {
    findNewWord(graph);
  }
}
