{
  Branch temp=root;
  for (int i=0; i < ints.length; i++) {
    if (i == ints.length - 1) {
      temp=temp.add(new Branch(ints[i],factory));
    }
 else {
      temp=temp.add(new Branch(ints[i],-1));
    }
  }
}
