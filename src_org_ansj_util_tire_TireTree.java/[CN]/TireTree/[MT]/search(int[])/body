{
  Branch temp=root;
  for (int i=0; i < ints.length; i++) {
    temp=temp.get(ints[i]);
    if (temp == null) {
      System.out.println("???????????????!!!!");
      return;
    }
  }
  print(temp);
}
