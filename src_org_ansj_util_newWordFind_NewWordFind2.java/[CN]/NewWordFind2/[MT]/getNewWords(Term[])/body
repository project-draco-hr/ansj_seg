{
  List<Node> words=null;
  List<Term> tempList=new ArrayList<Term>();
  for (  Term term : terms) {
    if (term == null) {
      continue;
    }
 else     if (filter(term)) {
      tempList.add(term);
    }
 else {
      if (tempList.size() > 1) {
      }
      if (tempList.size() > 0) {
        if (tempList.size() < 10) {
          tempList.clear();
        }
 else {
          tempList=new ArrayList<Term>();
        }
      }
    }
  }
  return pt;
  return words;
}
