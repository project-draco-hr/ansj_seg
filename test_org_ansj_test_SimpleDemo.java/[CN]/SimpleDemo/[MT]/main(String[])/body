{
  ToAnalysis.paser("init 123 ??????");
  int all=0;
  String str="Ansj??????????????????????????????ict?????????.????????????????????????????????????????????????????????????.????????????????????????????????????????????????!";
  long start=System.currentTimeMillis();
  for (int i=0; i < 100000; i++) {
    all+=str.length();
    ToAnalysis.paser(str);
  }
  System.out.println("???????????????:" + (all * 1000.0 / (System.currentTimeMillis() - start)));
}
