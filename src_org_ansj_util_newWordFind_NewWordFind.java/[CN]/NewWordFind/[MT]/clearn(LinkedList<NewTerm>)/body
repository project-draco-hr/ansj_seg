{
  Iterator<NewTerm> iterator=all.iterator();
  NewTerm temp=null;
  while (iterator.hasNext()) {
    temp=iterator.next();
    if (temp.isRemove()) {
      iterator.remove();
    }
 else {
      temp.clean();
    }
  }
}
