{
  if (nature != null && !"null".equals(nature.natureStr)) {
    if (this.getSubTerm() != null) {
      return this.getSubTerm().toString() + "/" + nature.natureStr;
    }
 else {
      return this.name + "/" + nature.natureStr;
    }
  }
 else {
    return this.name;
  }
}
